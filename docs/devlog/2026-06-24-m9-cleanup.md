---
id: focus-feed-devlog-2026-06-24-m9-cleanup
date: 2026-06-24
project: youtube-summary (Focus Feed)
pr: "#18"
tags: [devlog, cleanup, trend-filter, pwa, dead-code, tdd, parallel-worktree, code-review, merge]
---

# Dev Log — M9 잔여 정리 (PR #18, main 머지·검증 완료)

## 한 줄
M9 모바일 UX 대수술(PR #16) 후속 잔여(handoff `2026-06-24` §2) 중 코드 가능분 3건을 **병렬 worktree**로 처리하고, 트렌드 2토큰 정밀화는 **빈결과 회귀를 TDD로 실증해 보류**한 뒤 main에 squash 머지.

## 산출물 포인터
- PR: https://github.com/byh3071-cpu/youtube-summary/pull/18 (MERGED, squash → `2888542`)
- 진입점: 이 문서 → `docs/HANDOFF_2026-06-24_NEXT_SESSION.md` §2(갱신됨) → `docs/devlog/2026-06-24-mobile-ux-overhaul.md`(직전 PR #16)
- 주요 파일: `src/components/feed/WelcomeBanner.tsx`(삭제), `src/lib/filter.ts`, `src/lib/filter.test.ts`(신설), `src/components/PwaInstaller.tsx`

## 한 일
- **#1 데드코드 삭제** — `WelcomeBanner.tsx`: PR #16 홈 상단 개편으로 어디서도 import 안 되는 고아 컴포넌트. 소스 참조 0, orphan localStorage 키(`focus_feed_welcome_dismissed`) 읽기 0 확인 후 제거.
- **#3 트렌드필터 회귀테스트** — `filterFeedByTrendKeyword`에 단위테스트 18개 신설(`filter.test.ts`, vitest 82→100). phrase·sampleTitles(정/역방향+길이가드)·**비문자열 sample 가드(PAT-001/002)**·1/2/3토큰 threshold·ASCII 단어경계 vs 한국어 포함 매칭 커버.
- **#2 2토큰 threshold 정밀화 — 보류(증거기반)**: threshold 1→2(`>=2 ? 2 : 1`)는 과매칭을 줄이지만, `samples=[]` 경로에서 2토큰 중 1개씩만 맞는 현실 케이스가 빈결과 0으로 회귀함을 TDD로 실증. handoff의 "sample 매칭이 비-empty 보장" 가정은 samples가 실제 전달·매칭될 때만 참. → 현재 동작을 테스트로 잠그고 `filter.ts`에 보류 사유 2줄 주석만 추가.
- **#4 PWA dev 즉시정리** — `PwaInstaller`: dev에서 stale SW unregister + cache 삭제 후, 실제 SW가 있었으면(`regs.length > 0`) `sessionStorage` 가드된 1회 `location.reload()`로 "다음 로드"가 아닌 이번 세션 즉시 반영. CodeRabbit nitpick 반영해 cache 삭제를 unregister 체인 안으로 옮겨 reload 전 `await`(순차 보장).

## 검증
- 워크트리에 실제 `node_modules` 설치 후 전 게이트 통과: `vitest` 100(82→100) · `tsc` 0 · `eslint` 0 · `next build`(Turbopack) 0 · `vhk:policy`·`verify:focus-feed` PASS.
- 적대적 코드리뷰(다각도 finder + reviewer): 정합성 버그 0. CI(lint-build-test·Vercel·CodeRabbit) 전부 pass.

## 에러·교훈
- **교훈 1 (할일 = 명세, 맹목 적용 금지)**: handoff가 "threshold 올려라"라고 적었어도 그건 *가정*에 기댄 제안. 코드 실행으로 `samples=[]` 빈결과 회귀를 실증해 **보류**가 정답임을 증명. 핸드오프의 전제(여기선 "sample 비-empty 보장")는 코드로 검증한 뒤 따른다.
- **교훈 2 (worktree + Turbopack 함정)**: node_modules **junction**은 `tsc`/`eslint`/`vitest`는 통과시키지만 Next 16 `next build`(Turbopack)는 `Symlink ... points out of the filesystem root`로 거부. 워크트리 빌드는 junction이 아니라 **실 `node_modules` 설치**가 필요(또는 nested worktree). dev `--webpack`+junction 전역 hydration 실패와 별개 함정.
- **교훈 3 (테스트가 설계결정의 tripwire)**: 보류 같은 *의도적 트레이드오프*는 회귀테스트로 박아두면, 미래에 누가 threshold를 올릴 때 그 테스트가 깨지며 "왜 보류했는지"를 강제로 상기시킨다.
