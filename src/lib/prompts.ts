/** 자막 기반 3줄 요약 프롬프트 */
export const SUMMARY_PROMPT_CAPTION = (text: string) =>
  `다음은 유튜브 영상의 자막입니다. 영상을 직접 보지 않고 라디오처럼 듣기만 하는 직장인을 위해, 불필요한 서론을 빼고 핵심 정보(팩트 단위) 3가지를 명확하게 요약해주세요.\n\n자막 내용:\n${text.slice(0, 15000)}`;

/** 제목·설명 기반 3줄 요약 프롬프트 */
export const SUMMARY_PROMPT_SNIPPET = (text: string) =>
  `다음은 유튜브 영상의 제목과 설명입니다. 핵심 내용을 3줄 이내로 요약해주세요.\n\n내용:\n${text.slice(0, 15000)}`;

/** 재생성 시 품질 개선 힌트 (평가에서 미달 시 사용) */
export const SUMMARY_RETRY_HINT = (reason?: string) =>
  reason
    ? `\n\n[품질 개선 요청] 이전 요약이 다음 이유로 미달했습니다: ${reason}. 위 요구사항을 더 잘 반영해 다시 작성해주세요.`
    : "\n\n[품질 개선 요청] 이전 요약이 품질 기준에 미달했습니다. 핵심 3가지를 더 명확히, 불필요한 수식 없이 다시 작성해주세요.";

/** 요약 품질 평가 프롬프트 (평가자-최적화용). 원문 일부 + 생성된 요약 → 점수·통과 여부 */
export const getSummaryQualityEvalPrompt = (contextSnippet: string, summary: string) =>
  `당신은 유튜브 요약 품질 평가자입니다. 라디오처럼 듣기만 하는 직장인에게 도움이 되는지 기준으로 엄격히 평가하세요.

평가 기준 (모두 충족해야 고득점):
1) 핵심 정보(팩트)가 3가지로 명확히 구분되어 있으며, 각각 한 문장 이상으로 구체적으로 서술되어 있는가
2) 불필요한 서론·수식어·반복이 없고, 문장이 짧고 간결한가
3) 숫자·이름·키워드 등 구체적 팩트가 포함되어 있는가 (가능한 경우)
4) 듣기만 해도 바로 이해 가능한 문장인가
5) 원문(자막/제목·설명)과 내용이 일치하고, 과장·추측이 없는가

원문(일부):
${contextSnippet.slice(0, 3000)}

생성된 요약:
${summary}

위 5가지 기준으로 1~100 점수를 매기고, 78점 이상일 때만 passed를 true로 하세요. 반드시 아래 형식의 JSON만 반환하세요 (다른 설명·마크다운 없이):
{"score": 85, "passed": true, "reason": ""}
미달 시 예: {"score": 60, "passed": false, "reason": "핵심이 2가지만 구체적으로 드러남"}`;

/** 인사이트 재생성 시 품질 개선 힌트 */
export const INSIGHT_RETRY_HINT = (reason?: string) =>
  reason
    ? `\n\n[품질 개선 요청] 이전 인사이트가 다음 이유로 미달했습니다: ${reason}. 위 요구사항을 더 잘 반영해 다시 작성해주세요.`
    : "\n\n[품질 개선 요청] 이전 인사이트가 품질 기준에 미달했습니다. 핵심 인사이트 3가지와 실행 가능한 액션 2가지를 더 구체적으로 다시 작성해주세요.";

/** 인사이트 품질 평가 프롬프트 (평가자-최적화용) */
export const getInsightQualityEvalPrompt = (contextSnippet: string, insight: string) =>
  `당신은 유튜브 "인사이트 정리" 품질 평가자입니다. 직장인/지식 노동자가 영상을 본 뒤 배움과 액션을 정리하는 데 도움이 되는지 기준으로 엄격히 평가하세요.

평가 기준 (모두 충족해야 고득점):
1) 영상에서 건질 만한 핵심 인사이트가 3가지로 명확히 bullet 정리되어 있는가
2) 1주일 안에 시도해볼 수 있는 구체적인 액션이 2가지 bullet로 제안되어 있는가 (추상적 조언이 아닌 구체적 행동)
3) 말투가 존댓말 한국어이고, 동료 느낌으로 부담 없이 읽히는가
4) 원문(영상 내용)과 맞는 내용인가 (과장·무관한 내용 없음)
5) 인사이트와 액션이 서로 연결되어 있어, "배움 → 실행"이 자연스러운가

원문(일부):
${contextSnippet.slice(0, 3000)}

생성된 인사이트:
${insight}

위 5가지 기준으로 1~100 점수를 매기고, 78점 이상일 때만 passed를 true로 하세요. 반드시 아래 형식의 JSON만 반환하세요 (다른 설명·마크다운 없이):
{"score": 82, "passed": true, "reason": ""}
미달 시 예: {"score": 58, "passed": false, "reason": "액션이 추상적임"}`;

/** 인사이트 정리 프롬프트 */
export const INSIGHT_PROMPT = (text: string) =>
  `다음은 유튜브 영상의 내용입니다. 이 영상을 끝까지 본 시청자가
- 오늘 무엇을 배웠는지,
- 앞으로 일/공부/삶에 어떻게 적용하면 좋을지
정리할 수 있도록 도와주세요.

요구사항:
1) 영상에서 건질 만한 핵심 인사이트 3가지를, 한 줄씩 bullet로 정리
2) 한국 직장인/개발자/지식 노동자가 1주일 안에 실제로 시도해볼 수 있는 구체적인 액션 2가지를 bullet로 제안
3) 말투는 부담스럽지 않은 동료 느낌으로, 존댓말 한국어 사용

영상 내용:
${text.slice(0, 15000)}`;

/** 사용자의 목표/관심사 시스템 프롬프트 */
export const getSystemGoalPrompt = (goalText: string) =>
  `사용자의 목표/관심사:\n${goalText}\n\n이 사람은 1인 기업/프리랜서 시각에서, 오늘 당장 도움이 될 영상/글 위주로 우선순위를 정해줘야 합니다.`;

/** 피드 랭킹/브리핑 프롬프트 */
export const getRankingPrompt = (systemGoal: string, itemsPayload: string[], limit: number) => `
${systemGoal}

아래는 오늘 후보가 될 수 있는 콘텐츠 목록입니다. 각 JSON은 하나의 콘텐츠를 나타냅니다.

후보 목록:
${itemsPayload.join("\n")}

요구사항:
1) 사용자의 목표/관심사에 얼마나 직접적으로 도움이 되는지 기준으로 우선순위를 매기세요.
2) 가장 중요한 것부터 정렬해서 최대 ${Math.min(10, limit)}개만 선택하세요.
3) 각 항목에 대해:
   - priority: 1부터 시작하는 순위 (1이 가장 중요)
   - score: 1~100 사이 숫자로 중요도/적합도 점수
   - why: 이 사람이 이 콘텐츠를 지금 봐야 하는 이유 (한국어, 1~2문장)
   - action: 이 콘텐츠를 본 뒤 1주일 안에 실행해볼 수 있는 구체적인 액션 (한국어, 1문장)

반드시 아래 형식의 JSON만, 다른 자연어 설명이나 마크다운 없이 반환하세요:
{
  "items": [
    {
      "id": "<위 JSON 중 하나의 id>",
      "priority": 1,
      "score": 95,
      "why": "...",
      "action": "..."
    }
  ]
}
`;

/** 지능형 트렌드 레이더 프롬프트 */
export const getTrendRadarPrompt = (itemsPayload: string[]) => `
너는 뉴스/콘텐츠 트렌드 분석 전문가다.

아래는 최근 24시간 안에 올라온 유튜브·RSS 콘텐츠 목록이다. 각 줄은 하나의 JSON이며, title / sourceName / category / summary 필드를 가진다.

콘텐츠 목록:
${itemsPayload.join("\n")}

요구사항:
1) 위 콘텐츠들을 분석해, 서로 연관된 것들을 묶어서 상위 10개 이내의 "트렌드 키워드"를 뽑아라.
2) 각 트렌드는 아래 정보를 포함해야 한다.
   - keyword: 한국어로 2~6자 정도의 짧은 트렌드 이름 (예: "AI 에이전트", "Claude 업데이트")
   - score: 0~100 사이 숫자 (최근 24시간 기준 중요도/빈도)
   - summary: 이 트렌드가 무엇을 의미하는지, 왜 중요한지 한국어 1~2문장 설명
   - sampleTitles: 이 트렌드와 가장 잘 맞는 콘텐츠 제목 2~4개 (원문 제목 그대로)
3) 너무 일반적인 단어(예: "AI", "뉴스") 대신, 사용자가 오늘의 흐름을 한눈에 파악할 수 있는 구체적인 키워드를 사용하라.

반드시 아래 형식의 JSON만, 다른 자연어 설명이나 마크다운 없이 반환하라:
{
  "trends": [
    {
      "keyword": "예시 트렌드",
      "score": 87,
      "summary": "...",
      "sampleTitles": ["...", "..."]
    }
  ]
}
`;

/** 피드 Q&A: 병합 피드 일부를 컨텍스트로 질의. priorDialog는 직전 대화 요약(피드 원문이 우선). */
export const getFeedQAPrompt = (contentLines: string[], question: string, priorDialog = "") => `
너는 기술·비즈니스 뉴스 피드를 돕는 어시스턴트다.

아래 각 줄은 탭으로 구분된 필드다: ISO날짜, 소스명, 제목, 요약/설명 발췌(없으면 빈칸).

피드 항목:
${contentLines.join("\n")}
${priorDialog.trim() ? `\n이전 대화 참고(아래 피드 항목과 모순되면 반드시 피드 항목을 따른다):\n${priorDialog.trim()}\n` : ""}
사용자 질문(한국어):
${question}

규칙:
- 위 피드에 근거해 답하라. 목록에 없는 사실을 만들지 마라.
- 목록과 직접 관련 없으면 한두 문장으로 "이 피드 범위에서는 관련 항목을 찾지 못했다"고 답하라.
- 마크다운·목록 기호 없이 짧은 문단으로 답하라(800자 이내).
`;

